/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true, // React'in sıkı modunu etkinleştir
    swcMinify: true,       // SWC ile minify özelliğini etkinleştir
  };
  
  export default nextConfig;
  